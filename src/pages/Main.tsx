import {FC} from 'react';

interface MainProps {

}

export const Main: FC<MainProps> = (props) => {
    const {} = props;

    return (
        <div>
            Main
        </div>
    );
};

Main.displayName = 'Main';